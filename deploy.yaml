apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: deployment-name
spec:
  replicas: 1
  template:
    metadata:
      labels:
        label-key : label-value
    spec:
      containers:
        - name: angular-poq1et
          image: angular-poq1et:0.0.1 
          imagePullPolicy: Always
          ports:
          - containerPort: 80